package com.airport.domain.model;

/**
 * Attributes: hasLuxuryService, maxSpeed
 * Constructor: PrivateJet(model, capacity, fuelCapacity, hasLuxuryService, maxSpeed)
 * 1. Call the Aircraft constructor with model, capacity, and fuelCapacity.
 * 2. Set the hasLuxuryService and maxSpeed attributes.
 * Getter methods for hasLuxuryService and maxSpeed.
 */

public class PrivateJet {

}
